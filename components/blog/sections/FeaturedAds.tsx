import { BlogPostPreview } from "deco-sites/std/commerce/butterCMS/types.ts";

export interface Props {
  posts: BlogPostPreview[];
}

function FeaturedAds(children: Props) {
  return (
    <div>
    </div>
  );
}

export default FeaturedAds;
